    list<String> name=new list<String>{'veena','Aakriti','abhiti','amit','madav'};
    a=length(name);
    for(int i=0;i<a;i++)
    {
        if(name[i]=='Aakriti')
        {
            return 1;
        }
        else
        {
            return 0;
        }
    }

